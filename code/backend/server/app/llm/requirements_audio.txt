# Audio functionality requirements for the LLM module

# Required for audio playback
pygame>=2.1.0

# Optional: Local text-to-speech engine (fallback if Google Cloud is not available)
pyttsx3>=2.90

# Optional: Google Cloud Text-to-Speech (if you want to use Google Cloud instead of local TTS)
google-cloud-texttospeech>=2.15.0

# Note: google-cloud-texttospeech requires proper Google Cloud credentials to be configured
